package main

/*
	err := plugin.Load("sample/hello/hello.exe)"

	pl, err := plugin.Get("tag_hello")
	str, err := pl.Hello()         // str == "hello!"
	str, err := pl.Hello2("world") // str == "hello world!"

	plugin.Release()
*/
